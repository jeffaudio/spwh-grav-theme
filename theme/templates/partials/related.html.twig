{% import 'macros/cards.twig' as cards %}

<ul class="posts">
	{% for related_path, score in related_pages %}
		{% set related = grav['pages'].get(related_path) %}
		{% if related %}
			<li>{{ cards.postcard(related) }}</li>
		{% endif %}
	{% endfor %}
</ul>