{% set taxlist = taxonomylist.get() %}

{% if taxlist %}
	<div class="tags">
		{% for tax,value in taxlist['category']|slice(0,10) %}	
			<a class="boxed-link" href="{{ base_url }}/tag{{config.system.param_sep }}{{ tax|e('url')	 }}"><span>{{ tax|upper }}</span></a>
		{% endfor %}
	</div>

	<p><a href="{{ base_url }}/categories">View all categories…</a></p>
{% endif %}