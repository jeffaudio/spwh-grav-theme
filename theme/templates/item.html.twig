{% extends 'partials/base.html.twig' %}

{% set series = page.parent() %}
{% set isInSeries = series.parent().title != 'Pages' %}

{% if isInSeries %}
	{% set taxonomyRoot = series.parent.url %}
{% else %}
	{% set taxonomyRoot = page.parent.url %}
{% endif %}


{% block content %}
	<div class="row">
		<div class="small-12 columns">
			<header class="post">
				<h1 class="title"><a href="{{ page.url }}">{{ page.title }}</a></h1>
				<h3 class="subheading">
					{% if page.header.author %}By {{ page.header.author }} | {% endif %}
					{{ page.date|date("M. d, Y") }}
				</h3>

				{% if isInSeries %}
					<h3 class="subheading series">{{ series.title }}</h3>
				{% endif %}
			</header>

			<img class="feature-image" src="{{ page.media.images|first.url }}" />
		</div>
	</div>

	<div class="row">
		<main class="blog">
			<article class="post">
				<section class="content">
					{{ page.content }}
				</section>

				{% if isInSeries %}
					<div class="pagination-centered">
						<ul class="pagination" role="menubar" aria-label="Pagination">
							{% if not page.isLast %}
								<li><a href="{{ page.prevSibling.url }}">&laquo; {{ page.prevSibling.title }}</a></li>
							{% endif %}	

							{% if not page.isFirst %}
								<li><a href="{{ page.nextSibling.url }}">{{ page.nextSibling.title }} &raquo;</a></li>
							{% endif %}			
						</ul>
					</div>
				{% endif %}
	
				<footer>
					{% if page.taxonomy.category %}
						<p>Categories: {% for category in page.taxonomy.category %}<a href="{{ taxonomyRoot }}/category:{{ category }}" rel="category">{{ category }}</a>{% if not loop.last %}, {% endif %}{% endfor %}</p>
					{% endif %}
					{% include 'partials/social.html.twig' %}
				</footer>

				{% if page.taxonomy.tag %}
					<ul class="tags">
						{% for tag in page.taxonomy.tag %}
							<li class="tag"><a href="{{ taxonomyRoot }}/tag:{{ tag }}" rel="tag">{{ tag }}</a></li>
						{% endfor %}
					</ul>
				{% endif %}
			</article>

			{% if config.plugins.relatedpages.enabled and related_pages|length > 0 %}
    		{% include 'partials/related.html.twig' %}
			{% endif %}
		</main>

		<section class="sidebar">
			{% include 'partials/sidebar.html.twig' %}
		</section>
	</div>

	{% include 'partials/footer.html.twig' %}
{% endblock content %}
